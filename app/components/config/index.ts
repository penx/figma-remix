import styles from './config.css';
import { Config, ConfigPanel, ConfigField, ConfigFooter } from './config';
export {
  Config as Root,
  ConfigPanel as Panel,
  ConfigField as Field,
  ConfigFooter as Footer,
  styles,
};
